<section id="filterMenu">
	<nav class="navbar">
		<ul class="nav navbar-nav">
			<li class="nav-item filtercontent">Snappaajia: <%= page_entries_info @profiles, :model => 'profile' %> kpl</li>
			<span class="pull-sm-right">
			<%= form_for_filterrific @filterrific, html: { id: 'filterrific-no-ajax-auto-submit' } do |f| %>
			<li class="nav-item filtercontent">Näytä paikkakunnasta:
				<%= f.select :with_location_id, @filterrific.select_options[:with_location_id], { :include_blank => '- Kaikki -' } %>
			</li>
			<li class="nav-item filtercontent">Järjestä:
				<%= f.select :sorted_by,@filterrific.select_options[:sorted_by],{}%>
			</li>
			<li class="nav-item">
				<%= f.submit 'Hae', :class => 'submitfilter' %>
			</li>
			<% end %>
			</span>
		</ul>
	</nav>
	
	<!--<nav class="navbar">
	<button class="navbar-toggler hidden-sm-up" type="button" data-toggle="collapse" data-target="#filterMenuToggle">&#9776; Navikaatio</button>
	<div class="collapse navbar-toggleable-xs" id="filterMenuToggle">
		<%= link_to "", root_path, :class => "navbar-brand" %>
		<ul class="nav navbar-nav">
			<li class="nav-item">
			
			</li>
			<li class="nav-item">
				<a href="/about" class="nav-link">Mikä ihmeen Snappaajat.fi</a>
			</li>
			<% if user_signed_in? %>
			<li class="nav-item pull-right">
				<%= link_to "Omat profiilit", profiles_path, :class => "nav-link" %>
			</li>
			<% else %>
			<% end %>
		</ul>
	</div>
	</nav>-->
</section>

